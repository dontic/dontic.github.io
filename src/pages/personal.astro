---
import { getCollection } from 'astro:content';
import PageLayout from '~/layouts/PageLayout.astro';
import PostsByYear from '~/components/blog/PostsByYear.astro';

const publishedPosts = await getCollection('blog', ({ data }) => {
  return data.draft !== true;
});

// Filter posts by those that have the tag 'personal'
const personalPosts = publishedPosts.filter((post) => post.data.tags.includes('personal'));
---

<PageLayout>
  <PostsByYear posts={personalPosts} />
</PageLayout>
